{
  "name": "campus-crisis-poc",
  "version": "1.0.0",
  "private": true,
  "description": "Campus Crisis Decision-Support Engine - Real-Time AI & Deterministic Safety Engine",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --prefix apps/api & npm run dev --prefix apps/web",
    "dev:api": "npm run dev --prefix apps/api",
    "dev:web": "npm run dev --prefix apps/web",
    "build": "npm run build --prefix packages/shared && npm run build --prefix apps/api && npm run build --prefix apps/web",
    "test": "vitest run",
    "test:scenarios": "vitest run tests/scenarios",
    "lint": "eslint ."
  },
  "devDependencies": {
    "typescript": "^5.4.5",
    "vitest": "^1.6.0"
  }
}
